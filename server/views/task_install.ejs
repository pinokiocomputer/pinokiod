<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Install Task</title>
  <link href="/css/fontawesome.min.css" rel="stylesheet">
  <link href="/css/solid.min.css" rel="stylesheet">
  <link href="/css/brands.min.css" rel="stylesheet">
  <link href="/noty.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
  <link href="/task-launcher.css" rel="stylesheet">
  <% if (agent === "electron") { %>
    <link href="/electron.css" rel="stylesheet"/>
  <% } %>
</head>
<body class="<%= theme %> task-launcher-page task-page task-install-page" data-agent="<%= agent %>">
  <%- include('partials/app_navheader', { agent }) %>
  <main>
  <div class="task-container">
  <section class="task-shell">
    <header class="task-shell-header">
      <div class="task-shell-header-main">
        <p class="task-eyebrow">Tasks / Install</p>
        <h1 class="task-title">This task is not installed yet.</h1>
        <p class="task-description">Install this repo and Pinokio will reopen the same task from <code><%= typeof tasksRootPath === 'string' ? tasksRootPath : 'PINOKIO_HOME/tasks' %></code>.</p>
      </div>
      <div class="task-header-actions">
        <a class="task-link-button" href="/tasks">
          <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
          <span>Back</span>
        </a>
      </div>
    </header>

    <div class="task-shell-body">
      <% if (error) { %>
        <div class="task-status error"><%= error %></div>
      <% } %>

      <section class="task-section">
        <div class="task-section-head">
          <h2 class="task-section-title">Remote ref</h2>
          <div class="task-section-note">Canonical shared identity</div>
        </div>
        <pre class="task-code"><%= ref %></pre>
      </section>

      <% if (Object.keys(inputValues).length > 0 || selectedTool) { %>
        <section class="task-section">
          <div class="task-section-head">
            <h2 class="task-section-title">Launch context</h2>
            <div class="task-section-note">Kept after install</div>
          </div>
          <div class="task-context-list">
            <% if (selectedTool) { %>
              <div class="task-context-item"><strong>Tool</strong><span><%= selectedTool %></span></div>
            <% } %>
            <% Object.entries(inputValues).forEach(([name, value]) => { %>
              <div class="task-context-item"><strong><%= name %></strong><span><%= value %></span></div>
            <% }) %>
          </div>
        </section>
      <% } %>

      <form class="task-install-actions" method="post" action="/task/install">
        <input type="hidden" name="ref" value="<%= ref %>">
        <input type="hidden" name="returnTo" value="<%= returnTo %>">
        <button class="task-button primary" type="submit">
          <i class="fa-solid fa-download" aria-hidden="true"></i>
          <span>Install and continue</span>
        </button>
        <a class="task-link-button" href="/tasks">Cancel</a>
      </form>
    </div>
  </section>
  </div>
  <%- include('partials/main_sidebar', { selected: 'tasks' }) %>
  </main>
  <%- include('partials/app_common_scripts') %>
</body>
</html>
