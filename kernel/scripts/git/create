{
  "run": [{
    "method": "shell.run",
    "params": {
      "path": "{{args.cwd}}",
      "message": [
        "gh repo create {{args.name}} --{{args.visibility}} --source {{args.cwd}}"
      ]
    }
  }]
}
